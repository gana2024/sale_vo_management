<odoo>
<record id="view_crm_lead_form_inherit_vos_button" model="ir.ui.view">
    <field name="name">crm.lead.form.vo.button</field>
    <field name="model">crm.lead</field>
    <field name="inherit_id" ref="crm.crm_lead_view_form"/>
    <field name="arch" type="xml">
        <xpath expr="//div[contains(@class,'oe_button_box')]" position="inside">
            <button name="action_view_vos"
                    type="object"
                    class="oe_stat_button"
                    icon="fa-random"
                    modifiers="{'invisible': [('vo_count', '=', 0)]}">
                <field name="vo_count" widget="statinfo" string="VOs"/>
            </button>
        </xpath>
    </field>
</record>









    <record id="action_vo_from_crm" model="ir.actions.act_window">
    <field name="name">VOs</field>
    <field name="res_model">crm.lead</field>
    <field name="view_mode">tree,form</field>
    <field name="domain">
        [
            ('is_vo', '=', True),
            ('related_so_id.opportunity_id', '=', active_id)
        ]
    </field>
</record>

</odoo>
